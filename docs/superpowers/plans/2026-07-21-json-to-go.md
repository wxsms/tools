# JSON → Go Struct 工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 wxsm's Kit 中新增一个 JSON → Go struct 转换工具，用户粘贴 JSON 样本即可实时得到带 omitempty 选项的 Go 结构体定义。

**Architecture:** 新建 `src/tools/json-to-go/` 目录，包含 `json-to-go.js`（纯模块，封装 `quicktype-core` 异步调用）+ `JsonToGo.vue`（视图，镜像 `url-encode` 的双 textarea 布局）+ `json-to-go.test.js`（Vitest fixture 测试）。接线到 `router.js`、`routes.js`、`tools.js` 三处。开发在独立 feature 分支上进行。

**Tech Stack:** Vue 3 + Vite + Tailwind/DaisyUI + `quicktype-core` + Vitest + `@heroicons/vue`

---

## File Structure

**Create:**
- `src/tools/json-to-go/json-to-go.js` — 纯模块，导出 `jsonToGo(jsonString, { omitempty })`
- `src/tools/json-to-go/json-to-go.test.js` — Vitest 单元测试
- `src/tools/json-to-go/JsonToGo.vue` — 视图组件

**Modify:**
- `package.json` — 增加 `quicktype-core` 运行时依赖
- `src/router.js` — 注册 lazy 路由
- `src/routes.js` — 追加路由 meta
- `src/tools.js` — 在"编码转换"分组追加侧边栏条目

**Reference (read-only, 不改):**
- `src/tools/url-encode/UrlEncode.vue` — 双 textarea 转换器布局参考
- `src/tools/base64/base64.js` + `base64.test.js` — 纯函数 + 测试约定参考

---

## Task 0: 创建 feature 分支

**Files:** 无

- [ ] **Step 1: 从最新 master 创建并切换到 feature 分支**

Run:
```bash
git -C E:/githome-windows/tools checkout master
git -C E:/githome-windows/tools pull --ff-only
git -C E:/githome-windows/tools checkout -b feat/json-to-go
```

Expected: 切换到 `feat/json-to-go`，工作区干净。

- [ ] **Step 2: 确认起始状态**

Run:
```bash
git -C E:/githome-windows/tools status
git -C E:/githome-windows/tools branch --show-current
```

Expected: `On branch feat/json-to-go` + `nothing to commit, working tree clean`。

---

## Task 1: 安装 quicktype-core 依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装 quicktype-core**

Run:
```bash
cd E:/githome-windows/tools && npm install quicktype-core
```

Expected: `package.json` 的 `dependencies` 多出 `"quicktype-core": "^<version>"`，`package-lock.json` 更新。

- [ ] **Step 2: 验证 import 能成功**

Run:
```bash
cd E:/githome-windows/tools && node -e "import('quicktype-core').then(m => console.log(Object.keys(m).slice(0,5)))"
```

Expected: 打印 quicktype-core 导出的若干 key（包含 `quicktype`），无报错。

如果报错（例如依赖 Node 内置模块），记录错误信息后继续 —— 实测能 import 是后续步骤的前提，必须先解决。

- [ ] **Step 3: 提交**

Run:
```bash
git -C E:/githome-windows/tools add package.json package-lock.json
git -C E:/githome-windows/tools commit -m "chore(deps): add quicktype-core for json-to-go tool"
```

Expected: 一个新 commit。

---

## Task 2: 探测 quicktype-core 的 Go renderer 选项

**Files:** 无（只在终端验证，不改代码）

quicktype-core 的具体 `quicktype()` 入参结构、Go renderer 的 option 名以实测为准。本任务的产出是确认正确的 option 名，供 Task 3 使用。

- [ ] **Step 1: 写一个临时探测脚本**

Run（一次性内联命令，不需要建文件）:
```bash
cd E:/githome-windows/tools && node --input-type=module -e "
import { quicktype } from 'quicktype-core';
const r = await quicktype({
  inputData: JSON.stringify({ name: 'foo', age: 18, nested: { x: 1 } }),
  lang: 'go',
});
console.log('=== OUTPUT ===');
console.log(r.lines.join('\n'));
console.log('=== END ===');
"
```

如果 `quicktype({ inputData, lang })` 这套参数报错，尝试备选参数形态：
```bash
cd E:/githome-windows/tools && node --input-type=module -e "
import { quicktype } from 'quicktype-core';
const r = await quicktype({
  source: { kind: 'json', samples: [JSON.stringify({ name: 'foo', age: 18, nested: { x: 1 } })] },
  targetLanguage: 'go',
});
console.log(r.lines.join('\n'));
"
```

Expected: 打印一段包含 `type AutoGenerated struct`、`Nested` 等的 Go 代码。**记录最终能工作的入参形态**，Task 3 会用到。

- [ ] **Step 2: 探测 omitempty option**

在 Step 1 能工作的入参基础上，加 `rendererOptions`。先看 Go renderer 有哪些 option：
```bash
cd E:/githome-windows/tools && node --input-type=module -e "
import { getTargetLanguage } from 'quicktype-core';
const go = getTargetLanguage('go');
console.log(go.optionDefinitions.map(o => o.name));
"
```

Expected: 打印 Go renderer 支持的 option name 列表，通常包含 `just-types`、`just-types-and-omitempty` 之类。

- [ ] **Step 3: 用 omitempty option 重新跑一遍**

根据 Step 2 列出的 option name，跑：
```bash
cd E:/githome-windows/tools && node --input-type=module -e "
import { quicktype } from 'quicktype-core';
const r = await quicktype({
  // 用 Step 1 实测能工作的入参形态
  inputData: JSON.stringify({ name: 'foo', age: 18 }),
  lang: 'go',
  rendererOptions: { 'just-types-and-omitempty': true },
});
console.log(r.lines.join('\n'));
"
```

Expected: 输出包含 `omitempty`。若 option 名不是 `just-types-and-omitempty`，使用 Step 2 列出的正确名字，并记录下来。

**记录产物**：把"能工作的入参形态 + omitempty option 的正确名字"记在心里（或临时贴到 commit message 里），供 Task 3 使用。本任务不产生代码改动，不提交。

---

## Task 3: 写 `json-to-go.js` 的失败测试

**Files:**
- Create: `src/tools/json-to-go/json-to-go.test.js`

- [ ] **Step 1: 创建测试文件，写第一批 failing tests**

Create `src/tools/json-to-go/json-to-go.test.js`:
```js
import { describe, it, expect } from 'vitest'
import { jsonToGo } from './json-to-go.js'

describe('jsonToGo', () => {
  it('returns empty code for empty input', async () => {
    const r = await jsonToGo('')
    expect(r.ok).toBe(true)
    if (r.ok) expect(r.code).toBe('')
  })

  it('returns a clean error for invalid JSON', async () => {
    const r = await jsonToGo('{ not json')
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.error).toMatch(/JSON/)
  })

  it('generates Go struct from a flat object', async () => {
    const r = await jsonToGo(JSON.stringify({ name: 'foo', age: 18, active: true }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/type AutoGenerated struct/)
    expect(r.code).toMatch(/\bstring\b/)
    expect(r.code).toMatch(/\bint\b/)
    expect(r.code).toMatch(/\bbool\b/)
  })

  it('generates a separate struct for nested objects', async () => {
    const r = await jsonToGo(JSON.stringify({ outer: { inner: 1 } }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    // 两个 type ... struct 声明
    const matches = r.code.match(/type \w+ struct/g) || []
    expect(matches.length).toBeGreaterThanOrEqual(2)
  })

  it('emits omitempty when omitempty: true (default)', async () => {
    const r = await jsonToGo(JSON.stringify({ name: 'foo' }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toContain('omitempty')
  })

  it('does NOT emit omitempty when omitempty: false', async () => {
    const r = await jsonToGo(JSON.stringify({ name: 'foo' }), { omitempty: false })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).not.toContain('omitempty')
  })
})
```

- [ ] **Step 2: 运行测试确认它们 fail（因为模块还不存在）**

Run:
```bash
cd E:/githome-windows/tools && npx vitest run src/tools/json-to-go/json-to-go.test.js
```

Expected: FAIL，错误信息类似 `Failed to resolve import "./json-to-go.js"` 或 `jsonToGo is not a function`。

---

## Task 4: 实现 `json-to-go.js`

**Files:**
- Create: `src/tools/json-to-go/json-to-go.js`

- [ ] **Step 1: 写最小实现让测试通过**

Create `src/tools/json-to-go/json-to-go.js`:
```js
import { quicktype } from 'quicktype-core'

/**
 * @param {string} jsonString
 * @param {{ omitempty?: boolean }} [options]
 * @returns {Promise<{ ok: true, code: string } | { ok: false, error: string }>}
 *
 * NOTE: quicktype-core 的 quicktype() 入参形态、Go renderer 的 option 名以实测为准
 * （见 plan Task 2）。下面用的是 `inputData` + `lang` + `rendererOptions` 形态；
 * 若 Task 2 实测显示要用 `source`/`targetLanguage` 形态或不同 option 名，按实测调整。
 */
export async function jsonToGo(jsonString, { omitempty = true } = {}) {
  if (!jsonString || jsonString.trim() === '') {
    return { ok: true, code: '' }
  }

  let parsed
  try {
    parsed = JSON.parse(jsonString)
  } catch (e) {
    return { ok: false, error: 'JSON 解析失败：' + e.message }
  }

  try {
    const result = await quicktype({
      inputData: JSON.stringify(parsed),
      lang: 'go',
      rendererOptions: omitempty
        ? { 'just-types-and-omitempty': true }
        : { 'just-types': true },
    })
    return { ok: true, code: result.lines.join('\n') }
  } catch (e) {
    return { ok: false, error: '生成失败：' + e.message }
  }
}
```

**重要：实现时如果 Task 2 探测出的入参形态或 option 名与上面不同，按 Task 2 的实测结果改。** 函数签名 `jsonToGo(jsonString, { omitempty }) → { ok, code | error }` 不变。

- [ ] **Step 2: 运行测试确认通过**

Run:
```bash
cd E:/githome-windows/tools && npx vitest run src/tools/json-to-go/json-to-go.test.js
```

Expected: PASS。若失败，根据失败信息修正实现（最可能是 option 名或 quicktype 入参形态不对）。

- [ ] **Step 3: 提交**

Run:
```bash
git -C E:/githome-windows/tools add src/tools/json-to-go/json-to-go.js src/tools/json-to-go/json-to-go.test.js
git -C E:/githome-windows/tools commit -m "feat(json-to-go): add pure jsonToGo module with tests"
```

Expected: 一个新 commit，所有单测通过。

---

## Task 5: 实现 `JsonToGo.vue` 视图

**Files:**
- Create: `src/tools/json-to-go/JsonToGo.vue`

- [ ] **Step 1: 创建组件**

Create `src/tools/json-to-go/JsonToGo.vue`:
```vue
<template>
  <div>
    <h1 class="text-3xl font-bold mb-6">
      JSON → Go 转换
    </h1>

    <!-- Toolbar -->
    <div class="flex flex-wrap gap-2 mb-4">
      <label class="label cursor-pointer gap-2">
        <input
          v-model="omitempty"
          type="checkbox"
          class="checkbox checkbox-sm"
          @change="regenerate"
        >
        <span class="label-text">omitempty tag</span>
      </label>
    </div>

    <div class="flex flex-col gap-4 max-w-2xl">
      <!-- Input -->
      <div class="form-control">
        <label class="label"><span class="label-text font-semibold">JSON 输入</span></label>
        <div class="relative">
          <textarea
            v-model="input"
            class="textarea textarea-bordered w-full font-mono text-sm"
            placeholder="粘贴 JSON..."
            rows="8"
            @input="onInputChange"
          />
          <button
            v-if="input"
            class="btn btn-ghost btn-xs btn-square absolute bottom-2 right-2"
            :title="inputCopied ? '已复制！' : '复制'"
            @click="copyText(input, 'inputCopied')"
          >
            <CheckIcon
              v-if="inputCopied"
              class="w-4 h-4 text-success"
            />
            <ClipboardDocumentIcon
              v-else
              class="w-4 h-4"
            />
          </button>
        </div>
      </div>

      <div class="flex justify-center opacity-40">
        <ArrowsUpDownIcon class="w-6 h-6" />
      </div>

      <!-- Output -->
      <div class="form-control">
        <label class="label"><span class="label-text font-semibold">Go 输出</span></label>
        <div class="relative">
          <textarea
            v-model="output"
            class="textarea textarea-bordered w-full font-mono text-sm"
            placeholder="Go 结构体将在此处生成..."
            rows="12"
            readonly
          />
          <button
            v-if="output"
            class="btn btn-ghost btn-xs btn-square absolute bottom-2 right-2"
            :title="outputCopied ? '已复制！' : '复制'"
            @click="copyText(output, 'outputCopied')"
          >
            <CheckIcon
              v-if="outputCopied"
              class="w-4 h-4 text-success"
            />
            <ClipboardDocumentIcon
              v-else
              class="w-4 h-4"
            />
          </button>
        </div>
        <p
          v-if="error"
          class="text-error text-sm mt-1"
        >
          {{ error }}
        </p>
      </div>

      <div class="flex justify-end">
        <button
          class="btn btn-ghost btn-sm gap-1"
          @click="clear"
        >
          <TrashIcon class="w-4 h-4" />
          清空
        </button>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import {
  ArrowsUpDownIcon,
  ClipboardDocumentIcon,
  CheckIcon,
  TrashIcon,
} from '@heroicons/vue/24/outline'
import { jsonToGo } from './json-to-go.js'

const DEFAULT_INPUT = JSON.stringify({
  name: 'wxsm',
  age: 18,
  active: true,
  tags: ['dev', 'go'],
  address: { city: 'BJ', zip: '100000' },
}, null, 2)

const input = ref(DEFAULT_INPUT)
const output = ref('')
const error = ref('')
const omitempty = ref(true)
const inputCopied = ref(false)
const outputCopied = ref(false)

let debounceTimer = null

async function generate() {
  const r = await jsonToGo(input.value, { omitempty: omitempty.value })
  if (r.ok) {
    output.value = r.code
    error.value = ''
  } else {
    output.value = ''
    error.value = r.error
  }
}

function onInputChange() {
  error.value = ''
  if (debounceTimer) clearTimeout(debounceTimer)
  debounceTimer = setTimeout(generate, 300)
}

function regenerate() {
  // omitempty 切换：立即重跑，不 debounce
  generate()
}

function clear() {
  input.value = ''
  output.value = ''
  error.value = ''
}

async function copyText(text, flag) {
  try {
    await navigator.clipboard.writeText(text)
    if (flag === 'inputCopied') {
      inputCopied.value = true
      setTimeout(() => { inputCopied.value = false }, 1500)
    } else {
      outputCopied.value = true
      setTimeout(() => { outputCopied.value = false }, 1500)
    }
  } catch { /* clipboard not available */ }
}

onMounted(generate)
</script>
```

- [ ] **Step 2: 启动 dev server 验证页面能加载（手动）**

Run（在后台启动）:
```bash
cd E:/githome-windows/tools && npm run dev
```

然后在浏览器打开 `http://localhost:<port>/json-to-go`（此时路由还没注册 —— 跳过浏览器验证，改为下一步先接线再验证）。

实际上此步只做语法检查，不必真的启动 dev server。改用：保存文件后让 IDE/linter 报错（下一步 lint 会发现）。

- [ ] **Step 3: 暂不提交，等 Task 6 接线后一起验证再提交**

---

## Task 6: 接线 router / routes / tools.js

**Files:**
- Modify: `src/router.js`
- Modify: `src/routes.js`
- Modify: `src/tools.js`

- [ ] **Step 1: 在 `src/router.js` 注册 lazy 路由**

打开 `src/router.js`，在 `components` 对象里增加一行（建议放在 `/json` 之后保持视觉相邻）：

修改 `src/router.js:14` 附近，把：
```js
  '/json': lazy(() => import('./tools/json/Json.vue')),
```
之后追加：
```js
  '/json-to-go': lazy(() => import('./tools/json-to-go/JsonToGo.vue')),
```

- [ ] **Step 2: 在 `src/routes.js` 追加路由 meta**

打开 `src/routes.js`，在 `/json` 那一行（第 9 行）之后追加：
```js
  { path: '/json-to-go', meta: { title: 'JSON → Go', description: '将 JSON 样本转换为 Go 结构体定义，支持 omitempty' } },
```

- [ ] **Step 3: 在 `src/tools.js` 追加侧边栏条目**

打开 `src/tools.js`，在"编码转换"分组里（建议放在 `进制转换` 条目之后，作为该分组最后一项），追加：
```js
      {
        name: 'JSON → Go',
        path: '/json-to-go',
        desc: '将 JSON 样本转换为 Go 结构体定义',
        icon: CodeBracketSquareIcon,
      },
```

`CodeBracketSquareIcon` 已在 `src/tools.js:4` import，无需额外 import。

- [ ] **Step 4: lint 检查**

Run:
```bash
cd E:/githome-windows/tools && npm run lint
```

Expected: 无错误。若有，根据提示修复（通常是 trailing comma / 缩进）。

- [ ] **Step 5: 跑测试**

Run:
```bash
cd E:/githome-windows/tools && npm run test
```

Expected: 全部测试通过（包括 json-to-go 的 6 个测试 + 既有测试）。

- [ ] **Step 6: 启动 dev server 手动验证**

Run（后台）:
```bash
cd E:/githome-windows/tools && npm run dev
```

在浏览器打开 `http://localhost:<port>/json-to-go`，确认：
- 默认 JSON 预填示例能正确生成 Go 代码
- 切换 `omitempty` checkbox 后输出立即变化（包含/不包含 `omitempty`）
- 改 JSON 输入时输出有 ~300ms 防抖后更新
- 粘贴非法 JSON 时显示 `JSON 解析失败：...`
- 复制按钮、清空按钮工作正常
- 侧边栏"编码转换"分组下出现 `JSON → Go` 条目

- [ ] **Step 7: 提交**

Run:
```bash
git -C E:/githome-windows/tools add src/tools/json-to-go/JsonToGo.vue src/router.js src/routes.js src/tools.js
git -C E:/githome-windows/tools commit -m "feat(json-to-go): add JsonToGo view and wire route/sidebar"
```

Expected: 一个新 commit。

---

## Task 7: 最终验证 + 收尾

**Files:** 无

- [ ] **Step 1: 跑完整测试 + lint**

Run:
```bash
cd E:/githome-windows/tools && npm run lint && npm run test
```

Expected: 全绿。

- [ ] **Step 2: 跑生产构建确认无打包问题**

Run:
```bash
cd E:/githome-windows/tools && npm run build
```

Expected: 构建成功，无 quicktype-core 相关的打包警告/错误（quicktype-core 体积较大，会有 chunk size 提示但不应失败）。若有失败，根据错误信息排查（可能是 quicktype-core 依赖了 Node 内置模块需要 polyfill）。

- [ ] **Step 3: 确认分支提交历史**

Run:
```bash
git -C E:/githome-windows/tools log master..HEAD --oneline
```

Expected: 看到 3 个 commit：
- `chore(deps): add quicktype-core for json-to-go tool`
- `feat(json-to-go): add pure jsonToGo module with tests`
- `feat(json-to-go): add JsonToGo view and wire route/sidebar`

- [ ] **Step 4: 不创建 PR、不合并**

**重要：到此为止。** 用户会自己跑 `/code-review` 审查这个分支，再决定是否合并。**不要 `git push`、不要开 PR、不要 merge 到 master。**

向用户报告：feature 分支 `feat/json-to-go` 已就绪，3 个 commit 已落，测试 + lint + build 全绿，等待用户 `/code-review` 审查。

---

## Self-Review 检查

**Spec 覆盖：**
- ✅ quicktype-core 依赖 → Task 1
- ✅ 纯模块 `jsonToGo(json, { omitempty }) → { ok, code | error }` → Task 4
- ✅ 空输入短路、JSON parse 失败、quicktype 运行时失败 三种错误模式 → Task 4 实现里覆盖
- ✅ omitempty toggle → Task 5 + Task 4
- ✅ 镜像 url-encode 双 textarea 布局 → Task 5
- ✅ 预填示例 JSON → Task 5 `DEFAULT_INPUT`
- ✅ 输出只读、复制按钮、清空按钮 → Task 5
- ✅ debounce 300ms → Task 5
- ✅ 三处接线（router/routes/tools.js）→ Task 6
- ✅ Vitest fixture 测试，子串断言 → Task 3
- ✅ 不写 .component.test.js → Task 3 只写 .test.js
- ✅ 不在 master 上开发 → Task 0

**Placeholder scan：** 无 TBD / "add appropriate error handling" / 含糊引用。Task 2 是真实的探测任务，产出供 Task 3/4 使用；Task 4 的实现注明"按 Task 2 实测调整 option 名"是诚实的工程 note，不是 placeholder。

**Type consistency：** `jsonToGo(jsonString, { omitempty }) → { ok, code | error }` 签名在 Task 3 测试、Task 4 实现、Task 5 调用中一致。`omitempty` 默认 `true` 在三处一致。
