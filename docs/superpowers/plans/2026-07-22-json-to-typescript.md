# JSON → TypeScript 工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 wxsm's Kit 中新增一个独立的 `JSON → TypeScript` 工具，用户粘贴 JSON 即可实时得到 TypeScript 类型定义（`interface` 或 `type` 别名可切换），支持"所有属性可选"和自定义顶层类型名。

**Architecture:** 新建 `src/tools/json-to-ts/` 目录，包含 `json-to-ts.js`（纯函数，调 `quicktype-core` 生成 TS，签名 `jsonToTs(jsonString, { allOptional, preferTypes, typeName }) → { ok, code | error }`）+ `JsonToTs.vue`（视图，镜像 `JsonToGo.vue` 的双 CodeMirror 布局，工具栏多一个"用 type 别名"开关）+ `json-to-ts.test.js`（Vitest 单元测试）。接线到 `router.js`、`routes.js`、`tools.js` 三处。新增依赖 `@codemirror/lang-typescript`。开发在已有 `feat/json-to-typescript` 分支上继续。

**Tech Stack:** Vue 3 + Vite + Tailwind/DaisyUI + CodeMirror 6（已有的 `@codemirror/lang-json`、`@codemirror/theme-one-dark` + 新增 `@codemirror/lang-typescript`）+ `quicktype-core` + Vitest + `@heroicons/vue`

> 所有 `git -C` 命令在仓库根目录运行，用 `.` 表示当前仓库；不要硬编码机器绝对路径。如果当前 shell 不在仓库根，请先 `cd` 到仓库根再执行。

---

## File Structure

**Create:**
- `src/tools/json-to-ts/json-to-ts.js` — 纯模块，导出 `jsonToTs(jsonString, { allOptional = false, preferTypes = false, typeName = 'AutoGenerated' })`
- `src/tools/json-to-ts/json-to-ts.test.js` — Vitest 单元测试
- `src/tools/json-to-ts/JsonToTs.vue` — 视图组件

**Modify:**
- `package.json` — 新增 `@codemirror/lang-typescript` 依赖
- `src/router.js` — 增加懒加载路由
- `src/routes.js` — 追加路由 meta
- `src/tools.js` — 在"编码转换"分组追加侧边栏条目

**Reference (read-only, 不改):**
- `src/tools/json-to-go/JsonToGo.vue` — 双 CodeMirror 布局 + 主题切换 + 复制按钮的模式参考
- `src/tools/json-to-go/json-to-go.js` — `{ ok, code | error }` 返回约定参考
- `src/tools/json-to-go/json-to-go.test.js` — 测试风格参考

---

## Task 0: 确认起始分支状态

**Files:** 无

- [ ] **Step 1: 确认当前在 `feat/json-to-typescript` 分支且工作区干净**

Run:
```bash
git -C . status
git -C . branch --show-current
git -C . log --oneline -3
```

Expected:
- `On branch feat/json-to-typescript`
- `nothing to commit, working tree clean`
- 最近一笔提交是 spec doc：`docs(plan): add JSON → TypeScript implementation plan`

如果当前不在 `feat/json-to-typescript`，先 `git -C . checkout feat/json-to-typescript`。如果工作区有未提交改动，先处理（commit / stash）再继续。

---

## Task 1: 安装 `@codemirror/lang-typescript` 依赖

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`（由 npm 自动生成）

- [ ] **Step 1: 安装依赖**

Run:
```bash
npm install @codemirror/lang-typescript --save
```

Expected: `package.json` 的 `dependencies` 中出现 `"@codemirror/lang-typescript": "^6.x.x"`，`package-lock.json` 更新。

- [ ] **Step 2: 验证可导入**

Run:
```bash
node -e "import('@codemirror/lang-typescript').then(m => console.log(Object.keys(m).slice(0,5)))"
```

Expected: 输出包含 `typescript` 的数组，例如 `[ 'configureEditor', 'typescript', 'tsx', ... ]`。

- [ ] **Step 3: Commit**

```bash
git -C . add package.json package-lock.json
git -C . commit -m "$(cat <<'EOF'
chore(deps): add @codemirror/lang-typescript for json-to-ts tool

Co-Authored-By: zhipu/glm-5.2 <zai-org@claude-code-best.win>
EOF
)"
```

---

## Task 2: 创建纯模块 `json-to-ts.js`（TDD - 先写测试）

**Files:**
- Create: `src/tools/json-to-ts/json-to-ts.test.js`
- Create: `src/tools/json-to-ts/json-to-ts.js`

### Step 1: 写失败测试

- [ ] **Step 2.1: 创建测试文件 `src/tools/json-to-ts/json-to-ts.test.js`**

写入完整内容：

```js
import { describe, it, expect } from 'vitest'
import { jsonToTs } from './json-to-ts.js'

describe('jsonToTs', () => {
  it('returns empty code for empty input', async () => {
    const r = await jsonToTs('')
    expect(r.ok).toBe(true)
    if (r.ok) expect(r.code).toBe('')
  })

  it('returns empty code for whitespace-only input', async () => {
    const r = await jsonToTs('   \n\t  ')
    expect(r.ok).toBe(true)
    if (r.ok) expect(r.code).toBe('')
  })

  it('returns a clean error for invalid JSON', async () => {
    const r = await jsonToTs('{ not json')
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.error).toMatch(/JSON/)
  })

  it('generates TypeScript interface from a flat object', async () => {
    const r = await jsonToTs(JSON.stringify({ name: 'foo', age: 18, active: true }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/interface AutoGenerated/)
    expect(r.code).toMatch(/\bstring\b/)
    expect(r.code).toMatch(/\bnumber\b/)
    expect(r.code).toMatch(/\bboolean\b/)
  })

  it('generates a separate interface for nested objects', async () => {
    const r = await jsonToTs(JSON.stringify({ outer: { inner: 1 } }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    const matches = r.code.match(/interface \w+/g) || []
    expect(matches.length).toBeGreaterThanOrEqual(2)
  })

  it('emits optional fields when allOptional: true', async () => {
    const r = await jsonToTs(JSON.stringify({ name: 'foo', age: 18 }), { allOptional: true })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    // 至少有一个字段渲染为 `field?: type`
    expect(r.code).toMatch(/\?\s*:/)
  })

  it('does NOT emit optional fields when allOptional: false (default)', async () => {
    const r = await jsonToTs(JSON.stringify({ name: 'foo', age: 18 }))
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).not.toMatch(/\?\s*:/)
  })

  it('handles top-level array input', async () => {
    const r = await jsonToTs('[1, 2, 3]')
    expect(r.ok).toBe(true)
  })

  it('handles null field value', async () => {
    const r = await jsonToTs('{"a": null}')
    expect(r.ok).toBe(true)
  })

  it('handles mixed-type array', async () => {
    const r = await jsonToTs('[1, "a", true]')
    expect(r.ok).toBe(true)
  })

  it('uses custom typeName when provided', async () => {
    const r = await jsonToTs('{"a": 1}', { typeName: 'User' })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/interface User\b/)
    expect(r.code).not.toMatch(/interface AutoGenerated/)
  })

  it('falls back to AutoGenerated and returns warning when typeName is invalid', async () => {
    const r = await jsonToTs('{"a": 1}', { typeName: '1bad name!' })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/interface AutoGenerated/)
    expect(r.warning).toMatch(/合法 TypeScript 标识符/)
  })

  it('returns no warning when typeName is valid', async () => {
    const r = await jsonToTs('{"a": 1}', { typeName: 'User' })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.warning).toBeUndefined()
  })

  it('returns no warning when typeName is empty (uses default)', async () => {
    const r = await jsonToTs('{"a": 1}', { typeName: '' })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.warning).toBeUndefined()
  })

  it('outputs `type Foo = {...}` when preferTypes: true', async () => {
    const r = await jsonToTs('{"a": 1}', { preferTypes: true })
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/type AutoGenerated\s*=/)
    expect(r.code).not.toMatch(/interface AutoGenerated/)
  })

  it('outputs `interface Foo` when preferTypes: false (default)', async () => {
    const r = await jsonToTs('{"a": 1}')
    expect(r.ok).toBe(true)
    if (!r.ok) return
    expect(r.code).toMatch(/interface AutoGenerated/)
    expect(r.code).not.toMatch(/type AutoGenerated\s*=/)
  })
})
```

- [ ] **Step 2.2: 跑测试确认失败**

Run:
```bash
npm run test -- --run src/tools/json-to-ts/json-to-ts.test.js
```

Expected: FAIL，原因类似 `Failed to resolve import "./json-to-ts.js"` 或 `jsonToTs is not a function`。

### Step 2: 写最小实现

- [ ] **Step 2.3: 创建 `src/tools/json-to-ts/json-to-ts.js`**

写入完整内容：

```js
import { quicktype, jsonInputForTargetLanguage, InputData } from 'quicktype-core'

/**
 * @param {string} jsonString
 * @param {{ allOptional?: boolean, preferTypes?: boolean, typeName?: string }} [options]
 * @returns {Promise<{ ok: true, code: string, warning?: string } | { ok: false, error: string }>}
 *
 * quicktype-core TypeScript renderer 行为：
 * - `just-types: true`  只输出类型声明，不输出 runtime 解析代码
 * - `prefer-types: true` 输出 `type Foo = { ... }`；否则输出 `interface Foo { ... }`
 * - 顶层 option `allPropertiesOptional: true` 让所有字段渲染为 `field?: T`
 *
 * `typeName` 作为 `addSource({ name })` 传入，控制顶层类型名。非合法 TS 标识符时
 * 回退到 'AutoGenerated' 并返回 warning，让 UI 提示用户。
 */
const TS_IDENT_RE = /^[A-Za-z_$][A-Za-z0-9_$]*$/

export async function jsonToTs(jsonString, {
  allOptional = false,
  preferTypes = false,
  typeName = 'AutoGenerated',
} = {}) {
  if (!jsonString || jsonString.trim() === '') {
    return { ok: true, code: '' }
  }

  let parsed
  try {
    parsed = JSON.parse(jsonString)
  } catch (e) {
    return { ok: false, error: 'JSON 解析失败：' + e.message }
  }

  const trimmedName = (typeName || '').trim()
  const isValidName = trimmedName === '' || TS_IDENT_RE.test(trimmedName)
  const safeName = isValidName ? (trimmedName || 'AutoGenerated') : 'AutoGenerated'
  const warning = isValidName
    ? undefined
    : `结构体名 "${trimmedName}" 不是合法 TypeScript 标识符`

  try {
    const jsonInput = jsonInputForTargetLanguage('typescript')
    await jsonInput.addSource({ name: safeName, samples: [JSON.stringify(parsed)] })
    const inputData = new InputData()
    inputData.addInput(jsonInput)

    const rendererOptions = { 'just-types': true }
    if (preferTypes) rendererOptions['prefer-types'] = true
    const extraOptions = {}
    if (allOptional) extraOptions.allPropertiesOptional = true

    const result = await quicktype({
      lang: 'typescript',
      inputData,
      rendererOptions,
      ...extraOptions,
    })
    return { ok: true, code: result.lines.join('\n'), warning }
  } catch (e) {
    return { ok: false, error: '生成失败：' + e.message }
  }
}
```

### Step 3: 跑测试确认通过

- [ ] **Step 2.4: 跑全部测试**

Run:
```bash
npm run test -- --run src/tools/json-to-ts/json-to-ts.test.js
```

Expected: 所有用例 PASS。

如果 `allOptional: true` 的测试用例（Step 2.1 中 "emits optional fields"）失败，说明 `allPropertiesOptional` 在 TS renderer 下的输出形态与预期不符。检查实际输出：临时在 `json-to-ts.js` 里 `console.log(result.lines.join('\n'))`，跑 `jsonToTs('{"a":1}', { allOptional: true })`，看是 `a?:` 还是 `a: ... | undefined`。如果是后者，调整测试断言匹配 `| undefined` 而不是 `\?\s*:`，并把 spec "风险"段落的实测结论回填到 `json-to-ts.js` 顶部注释里。

- [ ] **Step 2.5: Commit**

```bash
git -C . add src/tools/json-to-ts/json-to-ts.js src/tools/json-to-ts/json-to-ts.test.js
git -C . commit -m "$(cat <<'EOF'
feat(json-to-ts): add pure jsonToTs converter with tests

Co-Authored-By: zhipu/glm-5.2 <zai-org@claude-code-best.win>
EOF
)"
```

---

## Task 3: 创建 Vue 视图 `JsonToTs.vue`

**Files:**
- Create: `src/tools/json-to-ts/JsonToTs.vue`

- [ ] **Step 3.1: 创建 `src/tools/json-to-ts/JsonToTs.vue`**

写入完整内容（基于 `JsonToGo.vue` 改造，工具栏多一个"用 type 别名"开关，输出编辑器换成 `@codemirror/lang-typescript`）：

```vue
<template>
  <div>
    <h1 class="text-3xl font-bold mb-6">
      JSON → TypeScript 转换
    </h1>

    <!-- Toolbar -->
    <div class="flex flex-wrap items-center gap-4 mb-4">
      <div class="form-control">
        <label class="label py-1"><span class="label-text">顶层类型名</span></label>
        <input
          v-model="typeName"
          type="text"
          class="input input-bordered input-sm w-48 font-mono"
          placeholder="AutoGenerated"
          @input="scheduleGenerate"
        >
      </div>
      <label class="label cursor-pointer gap-2">
        <input
          v-model="allOptional"
          type="checkbox"
          class="checkbox checkbox-sm"
          @change="regenerate"
        >
        <span class="label-text">所有属性可选</span>
      </label>
      <label class="label cursor-pointer gap-2">
        <input
          v-model="preferTypes"
          type="checkbox"
          class="checkbox checkbox-sm"
          @change="regenerate"
        >
        <span class="label-text">用 type 别名</span>
      </label>
    </div>

    <div class="flex flex-col gap-4 max-w-2xl">
      <!-- Input -->
      <div class="form-control">
        <label class="label"><span class="label-text font-semibold">JSON 输入</span></label>
        <div class="relative">
          <div
            ref="inputEditorEl"
            class="cm-container border border-base-300"
          />
          <button
            v-if="input"
            class="btn btn-ghost btn-xs btn-square absolute bottom-2 right-2 z-10"
            :title="inputCopied ? '已复制！' : '复制'"
            @click="copyText(input, 'inputCopied')"
          >
            <CheckIcon
              v-if="inputCopied"
              class="w-4 h-4 text-success"
            />
            <ClipboardDocumentIcon
              v-else
              class="w-4 h-4"
            />
          </button>
        </div>
        <p
          v-if="error"
          class="text-error text-sm mt-1"
        >
          {{ error }}
        </p>
        <p
          v-if="warning"
          class="text-warning text-sm mt-1"
        >
          {{ warning }}
        </p>
      </div>

      <div class="flex justify-center opacity-40">
        <ArrowDownIcon class="w-6 h-6" />
      </div>

      <!-- Output -->
      <div class="form-control">
        <label class="label"><span class="label-text font-semibold">TypeScript 输出</span></label>
        <div class="relative">
          <div
            ref="outputEditorEl"
            class="cm-container border border-base-300"
          />
          <button
            v-if="output"
            class="btn btn-ghost btn-xs btn-square absolute bottom-2 right-2 z-10"
            :title="outputCopied ? '已复制！' : '复制'"
            @click="copyText(output, 'outputCopied')"
          >
            <CheckIcon
              v-if="outputCopied"
              class="w-4 h-4 text-success"
            />
            <ClipboardDocumentIcon
              v-else
              class="w-4 h-4"
            />
          </button>
        </div>
      </div>

      <div class="flex justify-end">
        <button
          class="btn btn-ghost btn-sm gap-1"
          @click="clear"
        >
          <TrashIcon class="w-4 h-4" />
          清空
        </button>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted, onBeforeUnmount, watch, nextTick } from 'vue'
import { EditorView, keymap, lineNumbers, highlightActiveLineGutter, highlightActiveLine, drawSelection, rectangularSelection, highlightSpecialChars } from '@codemirror/view'
import { EditorState } from '@codemirror/state'
import { json as jsonLang } from '@codemirror/lang-json'
import { typescript as tsLang } from '@codemirror/lang-typescript'
import { oneDark } from '@codemirror/theme-one-dark'
import { defaultKeymap, history, historyKeymap, indentWithTab } from '@codemirror/commands'
import { syntaxHighlighting, defaultHighlightStyle, bracketMatching } from '@codemirror/language'
import { closeBrackets, closeBracketsKeymap } from '@codemirror/autocomplete'
import { searchKeymap, highlightSelectionMatches } from '@codemirror/search'
import {
  ArrowDownIcon,
  ClipboardDocumentIcon,
  CheckIcon,
  TrashIcon,
} from '@heroicons/vue/24/outline'
import { jsonToTs } from './json-to-ts.js'
import { useTheme } from '../../composables/useTheme.js'

const DEFAULT_INPUT = JSON.stringify({
  name: 'wxsm',
  age: 18,
  active: true,
  tags: ['dev', 'ts'],
  address: { city: 'BJ', zip: '100000' },
}, null, 2)

const input = ref(DEFAULT_INPUT)
const output = ref('')
const error = ref('')
const warning = ref('')
const allOptional = ref(false)
const preferTypes = ref(false)
const typeName = ref('')
const inputCopied = ref(false)
const outputCopied = ref(false)

const inputEditorEl = ref(null)
const outputEditorEl = ref(null)
let inputEditor = null
let outputEditor = null
let debounceTimer = null

const { theme } = useTheme()

function getThemeExt() {
  return theme.value === 'dark' ? oneDark : []
}

function createExtensions(langExt, onChange) {
  return [
    lineNumbers(),
    highlightActiveLineGutter(),
    highlightSpecialChars(),
    history(),
    drawSelection(),
    EditorState.allowMultipleSelections.of(true),
    syntaxHighlighting(defaultHighlightStyle, { fallback: true }),
    bracketMatching(),
    closeBrackets(),
    rectangularSelection(),
    highlightActiveLine(),
    highlightSelectionMatches(),
    keymap.of([
      ...closeBracketsKeymap,
      ...defaultKeymap,
      ...searchKeymap,
      ...historyKeymap,
      indentWithTab,
    ]),
    langExt,
    EditorView.updateListener.of(onChange),
    EditorView.theme({
      '&': { height: '100%' },
      '.cm-scroller': { overflow: 'auto' },
    }),
  ]
}

function inputOnChange(update) {
  if (!update.docChanged) return
  input.value = update.state.doc.toString()
  scheduleGenerate()
}

function outputOnChange() {
  // 输出是只读的，不处理变更
}

function createEditors() {
  if (inputEditorEl.value && !inputEditor) {
    inputEditor = new EditorView({
      state: EditorState.create({
        doc: input.value,
        extensions: [...createExtensions(jsonLang(), inputOnChange), getThemeExt()],
      }),
      parent: inputEditorEl.value,
    })
  }
  if (outputEditorEl.value && !outputEditor) {
    outputEditor = new EditorView({
      state: EditorState.create({
        doc: output.value,
        extensions: [
          ...createExtensions(tsLang(), outputOnChange),
          EditorState.readOnly.of(true),
          getThemeExt(),
        ],
      }),
      parent: outputEditorEl.value,
    })
  }
}

function destroyEditors() {
  inputEditor?.destroy()
  inputEditor = null
  outputEditor?.destroy()
  outputEditor = null
}

async function generate() {
  const r = await jsonToTs(input.value, {
    allOptional: allOptional.value,
    preferTypes: preferTypes.value,
    typeName: typeName.value.trim() || 'AutoGenerated',
  })
  if (r.ok) {
    error.value = ''
    warning.value = r.warning || ''
    output.value = r.code
    syncOutputEditor()
  } else {
    output.value = ''
    warning.value = ''
    error.value = r.error
    syncOutputEditor()
  }
}

function syncOutputEditor() {
  if (!outputEditor) return
  outputEditor.dispatch({
    changes: { from: 0, to: outputEditor.state.doc.length, insert: output.value },
  })
}

function scheduleGenerate() {
  error.value = ''
  warning.value = ''
  if (debounceTimer) clearTimeout(debounceTimer)
  debounceTimer = setTimeout(generate, 300)
}

function regenerate() {
  if (debounceTimer) {
    clearTimeout(debounceTimer)
    debounceTimer = null
  }
  generate()
}

function clear() {
  input.value = ''
  output.value = ''
  error.value = ''
  warning.value = ''
  if (inputEditor) {
    inputEditor.dispatch({
      changes: { from: 0, to: inputEditor.state.doc.length, insert: '' },
    })
  }
  syncOutputEditor()
}

async function copyText(text, flag) {
  try {
    await navigator.clipboard.writeText(text)
    if (flag === 'inputCopied') {
      inputCopied.value = true
      setTimeout(() => { inputCopied.value = false }, 1500)
    } else {
      outputCopied.value = true
      setTimeout(() => { outputCopied.value = false }, 1500)
    }
  } catch { /* clipboard not available */ }
}

watch(theme, async () => {
  destroyEditors()
  await nextTick()
  createEditors()
  syncOutputEditor()
})

onMounted(() => {
  createEditors()
  generate()
})

onBeforeUnmount(() => {
  if (debounceTimer) clearTimeout(debounceTimer)
  destroyEditors()
})
</script>

<style>
.cm-container {
  height: 320px;
  border-radius: var(--radius-field, 0.5rem);
  overflow: hidden;
}

.cm-container .cm-editor {
  height: 100%;
  font-size: 0.875rem;
}
</style>
```

- [ ] **Step 3.2: Commit**

```bash
git -C . add src/tools/json-to-ts/JsonToTs.vue
git -C . commit -m "$(cat <<'EOF'
feat(json-to-ts): add JsonToTs view

Co-Authored-By: zhipu/glm-5.2 <zai-org@claude-code-best.win>
EOF
)"
```

---

## Task 4: 接入路由和侧边栏

**Files:**
- Modify: `src/router.js`
- Modify: `src/routes.js`
- Modify: `src/tools.js`

- [ ] **Step 4.1: 修改 `src/router.js`，在 `/json-to-go-map` 后追加路由**

在 `components` 对象中，`'/json-to-go-map'` 这一行之后追加一行：

```js
  '/json-to-go-map': () => import('./tools/json-to-go-map/JsonToGoMap.vue'),
  '/json-to-ts': () => import('./tools/json-to-ts/JsonToTs.vue'),
```

- [ ] **Step 4.2: 修改 `src/routes.js`，在 `/json-to-go-map` 之后追加路由 meta**

```js
  { path: '/json-to-go-map', meta: { title: 'JSON → Go Map', description: '将 JSON 转换为 Go map[string]interface{} 字面量' } },
  { path: '/json-to-ts', meta: { title: 'JSON → TypeScript', description: '将 JSON 样本转换为 TypeScript 类型定义，支持 interface / type 别名切换' } },
```

- [ ] **Step 4.3: 修改 `src/tools.js`，在"编码转换"分组中 `JSON → Go Map` 之后追加侧边栏条目**

在 `JSON → Go Map` 条目之后追加：

```js
      {
        name: 'JSON → TypeScript',
        path: '/json-to-ts',
        desc: '将 JSON 样本转换为 TypeScript 类型定义，支持 interface / type 别名切换',
        icon: CodeBracketSquareIcon,
      },
```

`CodeBracketSquareIcon` 已在文件顶部 import（`json-to-go` / `json-to-go-map` 都在用），无需新增 import。

- [ ] **Step 4.4: 启动 dev server 手动验证**

Run:
```bash
npm run dev
```

打开浏览器访问 `http://localhost:5173/json-to-ts`，验证：
- 标题显示 "JSON → TypeScript 转换"
- 工具栏有三个控件：顶层类型名输入框、所有属性可选 checkbox、用 type 别名 checkbox
- 默认输入示例渲染，输出区显示 `interface AutoGenerated { ... }`，包含 `name: string` / `age: number` / `active: boolean` / `tags: string[]` 和嵌套 `address: Address`
- 勾选"所有属性可选"后字段变成 `name?: string` 之类
- 勾选"用 type 别名"后变成 `type AutoGenerated = { ... }`
- 在顶层类型名输入框填 `User`，输出变成 `interface User { ... }`
- 填 `1bad` 这种非法名，输出回退 `AutoGenerated`，下方显示黄色 warning
- 复制按钮、清空按钮工作正常
- 暗色主题切换不破坏编辑器

按 `Ctrl+C` 停止 dev server。

- [ ] **Step 4.5: Commit**

```bash
git -C . add src/router.js src/routes.js src/tools.js
git -C . commit -m "$(cat <<'EOF'
feat(json-to-ts): wire route, meta, and sidebar entry

Co-Authored-By: zhipu/glm-5.2 <zai-org@claude-code-best.win>
EOF
)"
```

---

## Task 5: Lint + 全量测试

**Files:** 无

- [ ] **Step 5.1: 跑 lint**

Run:
```bash
npm run lint
```

Expected: 无 error。如有 warning 视情况修复（仅限本次新增文件的 warning）。

- [ ] **Step 5.2: 跑全量测试**

Run:
```bash
npm run test
```

Expected: 全部 PASS，包括 `json-to-ts.test.js` 和其他既有测试。

- [ ] **Step 5.3: 跑 build 验证生产构建**

Run:
```bash
npm run build
```

Expected: 构建成功，无 TypeScript / Vue 编译错误。

如果 build 失败，根据报错修复（常见：import 路径写错、`@codemirror/lang-typescript` 没装上、Vue 模板语法错）。

---

## Self-Review 记录

**Spec coverage:**
- 文件结构（spec "文件结构"段）→ Task 2 + Task 3 + Task 4 ✓
- `jsonToTs` 签名和返回值（spec "纯函数"段）→ Task 2 ✓
- `allOptional` / `preferTypes` / `typeName` 三个选项 → Task 2 测试 + Task 3 Vue 工具栏 ✓
- Vue 视图三控件 + 双 CodeMirror + 复制/清空/主题切换 → Task 3 ✓
- `@codemirror/lang-typescript` 依赖 → Task 1 ✓
- 路由 + 侧边栏接线 → Task 4 ✓
- 测试用例 #1-#15（spec "测试"段）→ Task 2 Step 2.1 测试覆盖全部 ✓
- 风险段：`allPropertiesOptional` 输出形态实测 → Task 2 Step 2.4 失败处理路径 ✓

**Placeholder scan:** 所有步骤都有完整代码块或精确命令，无 TBD / TODO。

**Type consistency:** `jsonToTs` 函数签名在 Task 2 实现和 Task 3 Vue 调用两处一致（`allOptional` / `preferTypes` / `typeName`）。Vue 中 `typeName.value.trim() || 'AutoGenerated'` 与函数内部 `trimmedName || 'AutoGenerated'` 保持默认值一致。
